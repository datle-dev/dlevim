vim.treesitter.start()

-- -- nvim-treesitter indentation for python seems unpredictable, so omit
-- -- the following line and just rely on native indentation
-- vim.bo.indentexpr = "v:lua.require'nvim-treesitter'.indentexpr()"
